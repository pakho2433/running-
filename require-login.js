(() => {
  // Teacher login is temporarily disabled by request.
  // Keep this file as a safe no-op so cached index.html will not inject the teacher button.
  window.READING_RUN_TEACHER_LOGIN_DISABLED = true;
})();
